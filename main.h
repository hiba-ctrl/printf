#ifndef _MAIN_H
#define _MAIN_H

#include <stdarg.h>  /* For variable argument functions */
#include <stdlib.h>  /* For dynamic memory allocation, exit, etc. */
#include <unistd.h>  /* For write function */

int _printf(const char *format, ...);

#endif /* MAIN_H */
